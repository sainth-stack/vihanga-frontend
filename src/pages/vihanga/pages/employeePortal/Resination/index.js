import React from "react";
import { Box } from "@mui/material";
import ResignationManager from "./ResignationManager";

const Management = () => {
  return (
    <Box>
      <ResignationManager />
    </Box>
  );
};

export default Management;
